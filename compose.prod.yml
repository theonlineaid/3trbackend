version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: node_backend
    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env.production

    environment:
      - NODE_ENV=production
